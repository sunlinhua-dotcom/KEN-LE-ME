# ⚠️ 已弃用:AI key 已服务端化(见下方 GEMINI_API_KEY),前端不再读这些。
#   千万别把真实 key 填进 EXPO_PUBLIC_* —— 带这个前缀会被打进前端 bundle 泄露。
#   下面三行仅为历史保留/极少数本地直连脚本,正常部署留空即可。
# EXPO_PUBLIC_GEMINI_API_KEY=
# EXPO_PUBLIC_GEMINI_BASE_URL=https://api.apiyi.com/v1
# EXPO_PUBLIC_GEMINI_MODEL=gemini-3.5-flash

# ── 服务端转发源站(仅原生 App 需要;web 同源留空)──
# /api/fx(汇率)、/api/place(店铺口碑)与 /api/analyze 同源。
# 原生打包时设为部署源站,如 https://ken-le-me.pages.dev
EXPO_PUBLIC_API_BASE=

# ── 以下是「服务端」环境变量,配在 Cloudflare Pages 设置里,千万别带 EXPO_PUBLIC_ 前缀 ──
# 带前缀会被打进前端 bundle 泄露。这里只作说明,真实值勿写进 .env.example。
#   GEMINI_API_KEY      AI 鉴定 key(必填)
#   AMAP_KEY            高德 Web 服务 key —— 境内店铺评分(POI biz_ext.rating)
#   GOOGLE_PLACES_KEY   Google Places key —— 境外店铺评分(可选;不配则退化为 AI 参考口碑)
