# Cloudflare Pages 缓存策略(也兼容 Netlify)
# 带哈希的静态资源 → 永久缓存(文件名变了才会重取)
/_expo/static/*
  Cache-Control: public, max-age=31536000, immutable

# HTML 入口 → 不缓存,保证每次部署都能拿到最新
/
  Cache-Control: public, max-age=0, must-revalidate
/*.html
  Cache-Control: public, max-age=0, must-revalidate
